// frontend/js/config.js
const CONFIG = {
    // Add /health to the base URL? NO! Just use the base
    API_URL: 'https://triage-albania.onrender.com',  // NO /api here!
    
    // The API client will append endpoints
    // Examples:
    // - this.API_URL + '/api/health'  ✅ Works
    // - this.API_URL + '/api/auth/login' ✅ Works
};

// Or if your backend serves API from root:
const API_BASE = 'https://triage-albania.onrender.com';